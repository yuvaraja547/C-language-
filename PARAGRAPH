#include <stdio.h>
#include<string.h>

int main()
{
  char para[21];
  int c=0;
  printf("enter the paragraph:");
  gets(para);
  for(int i=0;i<strlen(para);i++)
  {
      if(para[i]==" " &&para[i+1]!=" ")
      {
          c=c+1;
          printf("%d",c);
      }
  }

    retu
